wb = xlsx_package.workbook
wb.add_worksheet(name: "Powders") do |sheet|
  sheet.add_row ["name", "pin_yin", "price_retail", "location", "qty_import"]
  @powders.each do |powder|
    sheet.add_row [powder.name, powder.pin_yin, powder.price_retail, powder.location, powder.qty_init]
  end
end
